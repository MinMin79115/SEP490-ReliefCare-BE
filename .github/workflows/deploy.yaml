name: Build and Deploy
on:
  push:
    branches: ["dev"]
  pull_request:
    branches: ["dev", "main"]
  workflow_dispatch:

jobs:
  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-22.04
    outputs:
      services: ${{ steps.changes.outputs.services }}
      reliefcare-api: ${{ steps.changes.outputs.reliefcare-api }}
      reliefcare-crawler: ${{ steps.changes.outputs.reliefcare-crawler }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 2

      - name: Check for changes
        id: changes
        run: |
          # Get list of changed files
          CHANGED_FILES=$(git diff --name-only HEAD^ HEAD 2>/dev/null || echo "")

          # If no previous commit (first push), build all
          if [ -z "$CHANGED_FILES" ]; then
            echo "First push detected - building all services"
            echo "reliefcare-api=true" >> $GITHUB_OUTPUT
            echo "reliefcare-crawler=true" >> $GITHUB_OUTPUT
            echo 'services=["reliefcare-api","reliefcare-crawler"]' >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "Changed files:"
          echo "$CHANGED_FILES"

          # Initialize services array
          SERVICES="["
          FIRST=true

          # Check reliefcare-api changes
          if echo "$CHANGED_FILES" | grep -E "^ReliefManagementSystem\.(API|Application|Domain|Infrastructure)/|^compose\.yaml$" > /dev/null; then
            echo "reliefcare-api=true" >> $GITHUB_OUTPUT
            if [ "$FIRST" = true ]; then
              SERVICES="${SERVICES}\"reliefcare-api\""
              FIRST=false
            else
              SERVICES="${SERVICES},\"reliefcare-api\""
            fi
          else
            echo "reliefcare-api=false" >> $GITHUB_OUTPUT
          fi

          # Check reliefcare-crawler changes
          if echo "$CHANGED_FILES" | grep -E "^RelieftManagement\.Crawler/|^compose\.yaml$" > /dev/null; then
            echo "reliefcare-crawler=true" >> $GITHUB_OUTPUT
            if [ "$FIRST" = true ]; then
              SERVICES="${SERVICES}\"reliefcare-crawler\""
              FIRST=false
            else
              SERVICES="${SERVICES},\"reliefcare-crawler\""
            fi
          else
            echo "reliefcare-crawler=false" >> $GITHUB_OUTPUT
          fi

          SERVICES="${SERVICES}]"
          echo "services=$SERVICES" >> $GITHUB_OUTPUT
          echo "Services to build: $SERVICES"

  api-image-build:
    name: Build / API
    runs-on: ubuntu-22.04
    needs: detect-changes
    if: needs.detect-changes.outputs.services != '[]'
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        service: ${{ fromJson(needs.detect-changes.outputs.services) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: DockerHub Login
        uses: docker/login-action@v3.1.0
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}

      - name: Lowercase repository name
        run: |
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          echo "REGISTRY=ghcr.io/$REPO_LOWER" >> $GITHUB_ENV

      - name: Build Image And Push
        run: |
          echo "Building service: ${{ matrix.service }}"
          docker compose -f compose.yaml --profile ${{ matrix.service }} build
          docker compose -f compose.yaml --profile ${{ matrix.service }} push

  api-image-deploy:
    name: Deploy / API
    runs-on: self-hosted
    needs: [api-image-build, detect-changes]
    if: needs.detect-changes.outputs.services != '[]'
    permissions:
      contents: read
      packages: write
    env:
      CLOUDFLARE_TUNNEL_TOKEN: ${{ secrets.CLOUDFLARE_TUNNEL_TOKEN }}
      POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
      JWT_KEY: ${{ secrets.JWT_KEY }}
      JWT_ISSUER: ${{ secrets.JWT_ISSUER }}
      JWT_AUDIENCE: ${{ secrets.JWT_AUDIENCE }}
      GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
      GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
      CORS_ORIGINS: ${{ secrets.CORS_ORIGINS }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: DockerHub Login
        uses: docker/login-action@v3.1.0
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}

      - name: Lowercase repository name
        shell: powershell
        run: |
          $repoLower = "${{ github.repository }}".ToLower()
          echo "REGISTRY=ghcr.io/$repoLower" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Pull and Deploy Changed Services
        shell: powershell
        run: |
          # Pull only the images that were built
          $services = '${{ needs.detect-changes.outputs.services }}' | ConvertFrom-Json

          foreach ($service in $services) {
            Write-Host "Pulling image for service: $service"
            docker compose -f "compose.yaml" -p "reliefcare-application" --profile $service pull
          }

          # Deploy all production services (this will use new images for changed services)
          Write-Host "Deploying application with updated services"
          docker compose -f "compose.yaml" -p "reliefcare-application" --profile production up -d

      - name: Wait for Application to be Ready
        shell: powershell
        run: |
          timeout /t 10 /nobreak
          docker compose -f "compose.yaml" -p "reliefcare-application" --profile production ps

      - name: Cleanup Images
        shell: powershell
        run: |
          docker image prune -a -f
